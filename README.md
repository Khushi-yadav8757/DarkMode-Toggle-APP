# DarkMode-Toggle-APP
A modern Dark Mode Toggle Web App built using HTML, CSS, and JavaScript.  The app allows users to switch between light and dark themes with smooth animations and saves the selected theme using LocalStorage.
